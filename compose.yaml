services:
#  postgres:
#    image: 'postgres:latest'
#    environment:
#      - 'POSTGRES_DB=mydatabase'
#      - 'POSTGRES_PASSWORD=secret'
#      - 'POSTGRES_USER=myuser'
#    ports:
#      - '5432'
  h2:
    image: oscarfonts/h2:latest
    ports:
      - "8082:8082"
      - "9092:9092"
    volumes:
      - h2-data:/opt/h2-data
    command: ["-baseDir", "/opt/h2-data", "-tcp", "-tcpAllowOthers", "-web", "-webAllowOthers"]

volumes:
  h2-data:
